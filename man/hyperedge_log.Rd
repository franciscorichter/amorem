% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hyperedge_log.R
\name{hyperedge_log}
\alias{hyperedge_log}
\alias{is_hyperedge_log}
\alias{as_hyperedge_log}
\alias{as_dyadic_log}
\title{Build / detect / convert hyperedge event logs}
\usage{
hyperedge_log(I, J, time)

is_hyperedge_log(x)

as_hyperedge_log(event_log)

as_dyadic_log(hyperedge_log)
}
\arguments{
\item{I}{List-column: each element is a character vector of sender
actor names participating in that event. Length-1 vectors are
allowed (and become standard dyadic events when combined with a
length-1 \code{J}).}

\item{J}{List-column: each element is a character vector of receiver
actor names. Empty character vectors are allowed and signal an
\emph{undirected} hyperevent.}

\item{time}{Numeric vector of event times. Must be finite and
non-decreasing after sorting.}

\item{x}{A data frame or list-of-columns to test or convert.}

\item{event_log}{A dyadic event log with \code{sender}, \code{receiver}, \code{time}
columns.}

\item{hyperedge_log}{A hyperedge log produced by \code{\link[=hyperedge_log]{hyperedge_log()}} or
\code{\link[=as_hyperedge_log]{as_hyperedge_log()}}.}
}
\value{
A \code{data.frame} with columns \code{I}, \code{J}, \code{time}, additionally
carrying class \code{amore_hyperedge_log} to distinguish it from a
dyadic log in dispatch contexts. Sorted by \code{time} ascending.
}
\description{
A \emph{hyperedge log} generalises the dyadic
\verb{(sender, receiver, time)} event log used elsewhere in \code{amore} to a
\verb{(I, J, time)} event log where \code{I} and \code{J} are list-columns
containing the set of senders and the set of receivers participating
in each hyperevent. This matches the data model of Boschi, Lerner &
Wit (2025): each event is a time-stamped directed hyperedge
\eqn{(t_m, I_m, J_m)}{(t_m, I_m, J_m)} from a sender set to a
receiver set.
}
\details{
The constructor \code{\link[=hyperedge_log]{hyperedge_log()}} accepts list-columns directly and
performs validation (character members, non-empty sets, finite
times, sorted by time). \code{\link[=as_hyperedge_log]{as_hyperedge_log()}} promotes a dyadic
\verb{(sender, receiver, time)} data frame to the hyperedge form by
wrapping each \code{sender} and \code{receiver} in a length-1 character
vector. \code{\link[=as_dyadic_log]{as_dyadic_log()}} is the inverse: it succeeds only when
every row of the hyperedge log has a length-1 sender set AND a
length-1 receiver set.

For \emph{undirected} hyperevents (e.g.\ multi-actor meetings), pass an
empty receiver set: \code{J = list(character(0), character(0), ...)}.
The receiver list-column must still be present.
}
\examples{
# Two co-authored citation events:
hl <- hyperedge_log(
  I    = list(c("alice", "bob"), c("alice", "carol")),
  J    = list(c("paperA"), c("paperA", "paperB")),
  time = c(1.0, 2.5))
is_hyperedge_log(hl)

# Round-trip a dyadic log:
dy <- data.frame(sender = c("a", "b"),
                 receiver = c("b", "c"),
                 time = c(1, 2))
h <- as_hyperedge_log(dy)
as_dyadic_log(h)
}
\references{
Boschi M, Lerner J, Wit EC (2025). \emph{Beyond Linearity and
Time-Homogeneity: Relational Hyper Event Models with Time-Varying
Non-Linear Effects}. arXiv:2509.05289.
}
