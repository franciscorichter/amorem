% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/subset_repetition.R
\name{hyperedge_subrep}
\alias{hyperedge_subrep}
\title{Subset repetition statistic for a hyperedge event log}
\usage{
hyperedge_subrep(
  hyperedge_log,
  I,
  J = character(0),
  t,
  rho = length(I),
  l = length(J)
)
}
\arguments{
\item{hyperedge_log}{A hyperedge log (see \code{\link[=hyperedge_log]{hyperedge_log()}}).}

\item{I}{Character vector of senders for the focal event.}

\item{J}{Character vector of receivers (or \code{character(0)} for
undirected).}

\item{t}{Focal time.}

\item{rho}{Order on the sender side: subset cardinality. Must be
between 1 and \code{length(I)}. Defaults to \code{length(I)} (full subset).}

\item{l}{Order on the receiver side: subset cardinality. Must be
between 0 and \code{length(J)}. Defaults to \code{length(J)} (full subset);
pass 0 to ignore receivers (undirected).}
}
\value{
A single non-negative numeric.
}
\description{
For a focal hyperedge \eqn{(t, I, J)}{(t, I, J)} and orders
\eqn{(\rho, \ell)}{(rho, l)}, computes the \strong{average activity} over
every sender subset of \code{I} of size \code{rho} and every receiver subset
of \code{J} of size \code{l}, per Boschi, Lerner & Wit (2025) Equation 4:
\deqn{
  \mathrm{subrep}^{\rho,\ell}(t,I,J)
  = \frac{1}{\binom{|I|}{\rho}\binom{|J|}{\ell}}
    \sum_{I' \subseteq I,\ |I'|=\rho}
    \sum_{J' \subseteq J,\ |J'|=\ell}
    \mathrm{activity}(t, I', J').
}
}
\details{
For dyadic events with \eqn{|I| = |J| = 1}{|I|=|J|=1},
\code{subrep(rho = 1, l = 1)} reduces to the dyad event count
(already exposed as \code{reciprocity_count} and related stats in
\code{\link[=compute_endogenous_features]{compute_endogenous_features()}}). The function exists because
for true hyperedge data the average over subsets of intermediate
size captures partial-subset repetition that no dyadic statistic
can represent.
}
\examples{
hl <- hyperedge_log(
  I    = list(c("a","b"), c("a","c"), c("b","c")),
  J    = list(c("X"),     c("X","Y"), c("Y")),
  time = c(1, 2, 3))
# Activity for the (a, X) sub-pair before t = 4:
hyperedge_activity(hl, I = "a", J = "X", t = 4)
# First-order subrep on event (a, b) -> X at t = 4:
hyperedge_subrep(hl, I = c("a","b"), J = "X", t = 4, rho = 1, l = 1)
}
\references{
Boschi M, Lerner J, Wit EC (2025). \emph{Beyond Linearity and Time-
Homogeneity: Relational Hyper Event Models with Time-Varying
Non-Linear Effects}. arXiv:2509.05289.
Lerner J, et al. (2025). The eventnet computation framework.
}
