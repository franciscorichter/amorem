% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/subset_repetition.R
\name{hyperedge_activity}
\alias{hyperedge_activity}
\title{Activity counter for hyperedge subsets}
\usage{
hyperedge_activity(hyperedge_log, I, J = character(0), t)
}
\arguments{
\item{hyperedge_log}{A hyperedge log (see \code{\link[=hyperedge_log]{hyperedge_log()}}).}

\item{I}{Character vector of sender names defining the focal subset.}

\item{J}{Character vector of receiver names defining the focal
subset. Pass \code{character(0)} to ignore the receiver side
(undirected events).}

\item{t}{Focal time. Only events strictly before \code{t} contribute.}
}
\value{
A single non-negative integer.
}
\description{
For a focal candidate hyperedge \eqn{(t, I, J)}{(t, I, J)},
\code{activity(t, I, J)} counts the number of past events
\eqn{(t_m, I_m, J_m)}{(t_m, I_m, J_m)} with \eqn{t_m < t}{t_m < t}
satisfying \eqn{I \subseteq I_m}{I in I_m} AND
\eqn{J \subseteq J_m}{J in J_m}.
}
\references{
Lerner J, Boschi M, Wit EC (2025). Subset repetition.
}
