% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rem.R
\name{rem}
\alias{rem}
\title{Fit a relational (hyper)event model on preprocessed case-control data}
\usage{
rem(
  formula,
  data,
  method = c("degenerate", "clogit"),
  case = "IS_OBSERVED",
  stratum = NULL,
  time = NULL,
  k = NULL,
  ...
)
}
\arguments{
\item{formula}{A formula; see \emph{Formula syntax}.}

\item{data}{A data.frame of preprocessed case-control data (wide for the
degenerate method; long with a case indicator and stratum for \code{clogit}).}

\item{method}{Estimation backend; see \emph{Description}.}

\item{case}{Name of the 0/1 event-indicator column (used by \code{clogit}).}

\item{stratum}{Name of the column grouping each case with its controls
(required by \code{clogit}).}

\item{time}{Name of the time column, required for \code{tve} / \code{tvnle} terms.}

\item{k}{Optional integer basis dimension passed to \code{s()} / \code{te()}.}

\item{...}{Reserved for future use.}
}
\value{
An object of class \code{"rem"}: a list with the fitted model (\verb{$fit}),
the \code{method}, the original \code{formula}, the parsed \code{terms}, and the number of
observations \code{n}. Has \code{\link[=summary]{summary()}}, \code{\link[=coef]{coef()}}, \code{\link[=plot]{plot()}} and \code{\link[=logLik]{logLik()}}
methods.
}
\description{
\code{rem()} is the unified front-end for fitting relational event models from
\strong{already preprocessed} case-control data (e.g. produced by \code{eventnet}),
where the endogenous/exogenous covariates have already been computed. It is
intended to supersede \code{\link[=compare_models]{compare_models()}}, \code{\link[=compare_models_smooth]{compare_models_smooth()}} and
\code{\link[=compare_models_global]{compare_models_global()}}, which couple feature computation and fitting.
}
\details{
Two estimation backends are provided:
\describe{
\item{\code{"degenerate"}}{Degenerate logistic regression on a case-1-control
design (Boschi, Lerner & Wit 2025): the response is a constant 1 and the
linear predictor is built from event-minus-control differences. Supports
smooth time-varying (\code{tve}), non-linear (\code{nle}) and time-varying
non-linear (\code{tvnle}) effects via \code{\link[mgcv:gam]{mgcv::gam()}}.}
\item{\code{"clogit"}}{Conditional logistic regression on a case-k-control
design via \code{\link[survival:clogit]{survival::clogit()}} (linear terms only). The case/control
strata are taken from \code{stratum}, or derived as \code{cumsum(case == 1)} when
\code{stratum} is \code{NULL} (assuming each case is immediately followed by its
controls, the eventnet blocked layout).}
}
}
\section{Formula syntax}{

The right-hand side lists covariates. A bare name is a \strong{linear} effect; wrap
a name to request a smooth effect (degenerate method only):
\itemize{
\item \code{tve(x)}   — time-varying linear effect: \code{s(time, by = d_x)}.
\item \code{nle(x)}   — non-linear effect: \code{s(cbind(x_ev, x_nv), by = c(1, -1))}.
\item \code{tvnle(x)} — time-varying non-linear effect (tensor product).
\item \code{re(x)}    — random intercept for a grouping factor \code{x} (one value
per row, e.g. the sender): \code{s(x, bs = "re")}. Note: in a matched
case-control design a random effect shared by a case and its control
(e.g. a sender that is identical within the matched pair) is only weakly
identified; review the parameterization for your design.
}
For the degenerate method the left-hand side is ignored (the response is the
constant case indicator); for \code{clogit} it is the 0/1 event indicator.
}

\section{Column resolution}{

For a covariate \code{x}, the event/control difference is taken from column \code{x},
else \code{d_x}, else \code{x_ev - x_nv}. Non-linear terms use \code{transform_x_ev} /
\code{transform_x_nv} when present (the eventnet spline-transformed covariate),
otherwise \code{x_ev} / \code{x_nv}. \code{tvnle} uses \code{transformed_time} when present.
Undirected logs (senders only, no receiver/\code{TARGET} column) are supported.
}

\examples{
set.seed(1)
w <- simulate_relational_events(
  n_events = 300, senders = paste0("a", 1:12), receivers = paste0("a", 1:12),
  n_controls = 1, endogenous_stats = "reciprocity_count",
  endogenous_effects = c(reciprocity_count = 0.6), wide = TRUE)
fit <- rem(~ reciprocity_count, data = w, method = "degenerate")
coef(fit)

}
\seealso{
\code{\link[=compare_models_smooth]{compare_models_smooth()}} (superseded), \code{\link[=simulate_relational_events]{simulate_relational_events()}}
(whose \code{wide = TRUE} output is a valid input here),
\code{\link[=simulate_directed_hyperevents_tvnl]{simulate_directed_hyperevents_tvnl()}}.
}
