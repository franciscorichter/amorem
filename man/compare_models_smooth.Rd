% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/compare_models_smooth.R
\name{compare_models_smooth}
\alias{compare_models_smooth}
\title{Compare candidate specifications with smooth (TV / NL / TVNL) effects}
\usage{
compare_models_smooth(
  event_log,
  models,
  scope = c("all", "appearance", "citation"),
  mode = c("one", "two"),
  half_life = NULL,
  k = NULL,
  seed = NULL,
  keep_fits = FALSE
)
}
\arguments{
\item{event_log}{Data frame with \code{sender}, \code{receiver}, \code{time} columns.}

\item{models}{Named list of specifications. Each entry is itself a
named character vector (or named list) mapping statistic names to
effect types: \code{"linear"}, \code{"tv"}, \code{"nl"}, or \code{"tvnl"}. Example:
\preformatted{
    list(
      linear = c(reciprocity_count   = "linear",
                 transitivity_count  = "linear"),
      nl    = c(reciprocity_time_recent  = "nl",
                 transitivity_time_recent = "nl"),
      tvnl  = c(reciprocity_time_recent  = "tvnl",
                 transitivity_time_recent = "tvnl"))
  }}

\item{scope, mode}{Passed through to \code{\link[=sample_non_events]{sample_non_events()}}.}

\item{half_life}{Required when an exp-decay statistic is requested.}

\item{k}{Optional integer: knot count for \code{s()} and \code{te()} terms.
Default \code{NULL} lets \code{mgcv} choose (\code{-1}).}

\item{seed}{Integer seed for the case-control sample.}

\item{keep_fits}{Logical; when \code{TRUE}, the returned table carries the fitted
model objects (one per spec, named by model, \code{NULL} for specs that failed)
as \code{attr(result, "fits")}, e.g. for plotting estimated effects. Defaults to
\code{FALSE}.}
}
\value{
Data frame with one row per specification and columns
\code{model}, \code{n_terms}, \code{n_obs}, \code{log_lik}, \code{AIC}, \code{delta_AIC}.
}
\description{
\strong{Superseded} by \code{\link[=rem]{rem()}}, which fits the same smooth (TV / NL / TVNL)
effects on preprocessed case-control data. \code{compare_models_smooth()} remains
fully supported.

Mirrors \code{\link[=compare_models]{compare_models()}} but lets each statistic in a specification
take one of four effect types instead of a single linear coefficient:
linear, time-varying (TV), non-linear (NL), or jointly time-varying
non-linear (TVNL). The smooth machinery follows
Boschi, Lerner & Wit (2025); the matrix-of-event-vs-non-event trick
is documented in their Section 3.3.

For each specification:
\itemize{
\item One case-control sample is drawn from \code{event_log} with
\code{n_controls = 1} (paired event / non-event design).
\item For every requested statistic, both the case (event) and the
control (non-event) features are computed via
\code{\link[=compute_endogenous_features]{compute_endogenous_features()}}.
\item The mgcv design uses the case-vs-control matrix trick:
\itemize{
\item linear -> a single coefficient on \code{case - control} (column \code{d_stat}).
\item tv    -> \code{s(time, by = d_stat)} — smooth in time, multiplied by \code{d_stat}.
\item nl    -> \code{s(stat_mat, by = I_mat)} where \code{stat_mat} is a
two-column matrix \code{cbind(case, control)} and \code{I_mat} is
\code{cbind(1, -1)}.
\item tvnl  -> \code{te(time_mat, stat_mat, by = I_mat)} tensor product
smooth, with time_mat both columns equal to the event time vector.
}
\item The model is fitted with \code{mgcv::gam} and a degenerate logistic
likelihood: response = \code{rep(1, n)}, formula = \code{one ~ -1 + ...},
\code{family = binomial}. This matches Boschi et al. equation 8.
}

AIC values are directly comparable across specifications because every
fit uses the same case-control sample. Returns the same tidy
\code{data.frame} as \code{\link[=compare_models]{compare_models()}}.
}
\examples{
\dontrun{
data(classroom_events)
compare_models_smooth(
  classroom_events,
  models = list(
    linear = c(reciprocity_time_recent  = "linear",
               transitivity_time_recent = "linear"),
    nl    = c(reciprocity_time_recent  = "nl",
               transitivity_time_recent = "nl"),
    tvnl  = c(reciprocity_time_recent  = "tvnl",
               transitivity_time_recent = "tvnl")),
  seed = 11)
}
}
\references{
Boschi M, Lerner J, Wit EC (2025). \emph{Beyond Linearity and Time-Homogeneity:
Relational Hyper Event Models with Time-Varying Non-Linear Effects}.
arXiv:2509.05289.
}
\seealso{
\code{\link[=compare_models]{compare_models()}} for the linear-only variant.
}
