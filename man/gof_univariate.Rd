% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gof.R
\name{gof_univariate}
\alias{gof_univariate}
\title{Goodness-of-fit test for a single FLE covariate}
\usage{
gof_univariate(
  event_log,
  model,
  covariate,
  scope = "all",
  mode = "one",
  half_life = NULL,
  seed = NULL
)
}
\arguments{
\item{event_log}{Dyadic event log.}

\item{model}{Named character vector of \verb{<stat> = "linear"} mapping.}

\item{covariate}{Name of the covariate in \code{model} to test.}

\item{scope, mode, half_life, seed}{See \code{\link[=compare_models]{compare_models()}}.}
}
\value{
A list with \code{statistic} (\eqn{T_x}{T_x}), \code{p_value} (KS),
\code{W} (numeric vector of length \code{n}, the normalised process),
and \code{u} (the time grid in \verb{[0, 1]}).
}
\description{
Implements the univariate cumulative martingale residual test of
Boschi & Wit (2025), Section 3.3. The test statistic is
\eqn{T_x = \sup_u |\hat W[u]|}{T_x = sup_u |W[u]|} where
\eqn{\hat W[u]}{W[u]} is the normalised cumulative score process for
the requested covariate; under correct specification \eqn{\hat W}{W}
converges to a standard Brownian bridge, so the p-value follows the
Kolmogorov-Smirnov distribution
\eqn{2 \sum_{k\ge 1} (-1)^{k-1} e^{-2 k^2 t^2}}{2 sum (-1)^(k-1) exp(-2 k^2 t^2)}.
}
\examples{
\dontrun{
data(classroom_events)
gof_univariate(classroom_events,
  model = c(reciprocity_count  = "linear",
            transitivity_count = "linear"),
  covariate = "reciprocity_count", seed = 1)
}
}
\references{
Boschi M, Wit EC (2025). \emph{Goodness of fit in relational
event models}. Statistics and Computing 36(4).
}
