% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/compare_models_global.R
\name{compare_models_global}
\alias{compare_models_global}
\title{Compare REM specifications with global covariate effects}
\usage{
compare_models_global(
  event_log,
  models,
  global_covariates = NULL,
  scope = c("all", "appearance", "citation"),
  mode = c("one", "two"),
  half_life = NULL,
  shift_scale = 1,
  k = NULL,
  k_cyclic = 10,
  seed = NULL,
  keep_fits = FALSE
)
}
\arguments{
\item{event_log}{Data frame with \code{sender}, \code{receiver}, \code{time}.}

\item{models}{Named list of specifications (see "Specification
format" above).}

\item{global_covariates}{Optional data frame with a \code{time} column
plus one column per global covariate referenced in \code{models}.
The function evaluates each covariate at the focal event time
and at the non-event's shifted time by stepwise lookup (LOCF on
the \code{time} axis).}

\item{scope, mode}{Passed through to \code{\link[=sample_non_events]{sample_non_events()}}.}

\item{half_life}{Required when any dyadic spec uses an exp-decay
stat.}

\item{shift_scale}{Multiplier on the average inter-arrival time
for the exponential shift distribution. Defaults to 1.}

\item{k}{Optional knot count for smooth terms (see
\code{\link[mgcv:s]{mgcv::s()}}). Defaults to \code{mgcv}'s automatic choice.}

\item{k_cyclic}{Knot count for \code{global_cyclic} smooths
(paper uses 10 for time-of-day).}

\item{seed}{Integer seed for the case-control sample and the
shift draws.}

\item{keep_fits}{Logical; when \code{TRUE}, the returned table carries the fitted
model objects (one per spec, named by model, \code{NULL} for specs that failed)
as \code{attr(result, "fits")}, e.g. for plotting estimated effects. Defaults to
\code{FALSE}.}
}
\value{
Data frame with one row per specification and columns
\code{model}, \code{n_terms}, \code{n_obs}, \code{log_lik}, \code{AIC}, \code{delta_AIC}.
}
\description{
\strong{Superseded} by \code{\link[=rem]{rem()}}, the unified front-end for fitting relational
event models on preprocessed case-control data. \code{compare_models_global()}
remains fully supported.

Implements the time-shifted partial likelihood of Lembo,
Juozaitienė, Vinciotti & Wit (2025) for fitting relational event
models with \strong{global covariate effects} — covariates that are
time-dependent but constant across all interacting pairs (e.g.\
temperature, time of day, the residual baseline hazard). Standard
case-control partial likelihood cannot identify these because
global terms cancel in the rate ratio; this function follows the
paper's Section 4 recipe: a random per-dyad time shift breaks the
cancellation, and with one non-event per event the partial
likelihood reduces to a degenerate logistic additive model fit by
\code{\link[mgcv:gam]{mgcv::gam()}}.

Per the paper's equations 11-13:
\deqn{
  \mathcal{L}^{PS}(f, g) = \prod_{k=1}^n
    \frac{\exp\{\Delta_k(f; x_{s_k r_k}) + \Delta_k(g; x_k)\}}
         {1 + \exp\{\Delta_k(f; x_{s_k r_k}) + \Delta_k(g; x_k)\}}
}{
  L^PS(f, g) = prod_k logistic(Delta_k(f; x_{s_k r_k}) + Delta_k(g; x_k))
}
where each \eqn{\Delta_k}{Delta_k} is the difference between the
(smooth) function evaluated at the focal event time and at the
sampled non-event's \emph{shifted} time
\eqn{t^*_k = t_k - h_{s^* r^*}}{t*_k = t_k - h_{s*r*}}.

Shift distribution. Per-dyad shifts \eqn{H_{sr}}{H_sr} are drawn
independently from an exponential distribution with mean
\eqn{\nu \cdot \bar{\Delta t}}{nu * mean_dt} where
\eqn{\bar{\Delta t}}{mean_dt} is the average inter-arrival time
in \code{event_log}. The paper's simulation studies find that
\eqn{\nu = 1}{nu = 1} works in practice and that the estimates
are robust to choices in \eqn{[0.1, 10]}{[0.1, 10]}.

Specification format. Each entry of \code{models} is a named character
vector mapping a covariate name (a statistic in
\code{\link[=compute_endogenous_features]{compute_endogenous_features()}} \strong{or} a column of
\code{global_covariates}) to an effect type:
\itemize{
\item \code{"linear"} -- linear \code{beta * x} term.
\item \code{"nle"}     -- smooth \code{s(x)} (thin-plate, paper's default).
\item \code{"tve"}     -- smooth \code{s(time, by = x)} (time-varying).
\item \code{"tvnle"}   -- tensor product \code{te(time, x)}.
\item \code{"global_smooth"} -- smooth \code{s(x_global)} evaluated at the
focal time vs. the non-event's shifted time (the paper's
\code{g_b(x^{(b)}(t))} family).
\item \code{"global_cyclic"} -- cyclic smooth \code{s(x_global, bs = "cc")}
for time-of-day-like covariates with a periodic domain.
\item \code{"global_time"} -- a smooth on \code{time} itself, recovering
the residual time effect \eqn{g_0(t)}{g_0(t)} of paper eq. 3.
}
}
\examples{
\dontrun{
data(classroom_events)
# Hourly temperature track on the same time axis:
g <- data.frame(time = seq(0, max(classroom_events$time), length = 50),
                temperature = rnorm(50, 20, 5))
compare_models_global(
  classroom_events,
  models = list(
    dyadic_only = c(reciprocity_count        = "linear",
                    transitivity_count       = "linear"),
    with_global = c(reciprocity_count        = "linear",
                    transitivity_count       = "linear",
                    temperature              = "global_smooth",
                    time                     = "global_time")),
  global_covariates = g,
  seed = 11, k = 5)
}
}
\references{
Lembo M, Juozaitienė R, Vinciotti V, Wit EC (2025).
\emph{Relational event models with global covariates: an application
to bike sharing}. Journal of the Royal Statistical Society,
Series C. \doi{10.1093/jrsssc/qlaf058}.
}
\seealso{
\code{\link[=compare_models]{compare_models()}} (linear, no globals),
\code{\link[=compare_models_smooth]{compare_models_smooth()}} (smooth dyadic effects, no globals).
}
