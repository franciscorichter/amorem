% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/preprocess.R
\name{sample_non_events}
\alias{sample_non_events}
\title{Sample non-events for inference}
\usage{
sample_non_events(
  event_log,
  n_controls = 1,
  scope = c("all", "appearance", "citation"),
  mode = c("two", "one"),
  risk = c("standard", "remove"),
  exclude_pairs = NULL,
  allow_loops = FALSE,
  seed = NULL,
  max_attempts = 1000
)
}
\arguments{
\item{event_log}{Data frame with columns \code{sender}, \code{receiver}, and \code{time}.}

\item{n_controls}{Number of non-events (controls) to sample per realized
event.}

\item{scope}{Candidate set definition. \code{"all"} uses every actor observed in
the data; \code{"appearance"} restricts to actors that have appeared in prior
events; \code{"citation"} matches citation networks where senders are restricted
to the papers that debut at the current time and receivers must have
appeared earlier.}

\item{mode}{\code{"one"} draws both sender and receiver from the same candidate
pool (single-mode). \code{"two"} samples sender and receiver from separate pools
(two-mode).}

\item{risk}{Strategy governing the risk set. \code{"standard"} (default) keeps all
unrealized dyads available across strata, whereas \code{"remove"} deletes a dyad
from the candidate pool after it has occurred (useful for processes such as
species invasions where a pair cannot reoccur). Under \code{"remove"}, dyads
firing at the focal event's own timestamp are also kept out of its control
pool (concurrent events are not valid non-events at that instant).}

\item{exclude_pairs}{Optional two-column data.frame/matrix of
\verb{(sender, receiver)} pairs that are structurally ineligible as controls and
must never be sampled (e.g. an alien species' native range, or any dyad
forbidden in advance). Columns named \code{sender}/\code{receiver} are used if
present, otherwise the first two columns.}

\item{allow_loops}{Logical; can sampled non-events have identical sender and
receiver?}

\item{seed}{Optional seed for reproducibility.}

\item{max_attempts}{Maximum resampling attempts per control before giving up
(prevents infinite loops when candidate sets are small).}
}
\value{
A data.frame containing the original events (\code{event = 1}) and the
sampled controls (\code{event = 0}), grouped by \code{stratum} identifiers.
}
\description{
Given an observed event log, generate nested case-control data by sampling
counterfactual sender--receiver pairs according to predefined strategies.
}
