% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rem_nn_uncertainty.R
\name{nn_uncertainty}
\alias{nn_uncertainty}
\title{Bootstrap uncertainty for the neural rem() backend}
\usage{
nn_uncertainty(
  object,
  data,
  B = 200L,
  case = NULL,
  stratum = NULL,
  n_grid = 50L,
  level = 0.95,
  seed = NULL
)
}
\arguments{
\item{object}{A fitted \code{\link[=rem]{rem()}} object with \code{method = "nn"}.}

\item{data}{The case-control data frame the model was fit on (same columns).}

\item{B}{Number of bootstrap resamples.}

\item{case, stratum}{Event-indicator and stratum columns, resolved exactly as
in \code{\link[=rem]{rem()}} (defaults: the formula's left-hand side, and
\code{cumsum(case == 1)}).}

\item{n_grid}{Grid resolution for the partial-dependence curves.}

\item{level}{Confidence level for the bands and the concordance interval.}

\item{seed}{Optional integer seed for the resampling.}
}
\value{
An object of class \code{"nn_uncertainty"}: a per-feature list of
\code{data.frame(x, lo, med, hi)} bands, a \code{concordance} quantile interval, and
the settings \code{B}, \code{level}. Has \code{print()} and \code{plot()} methods.
}
\description{
Quantifies uncertainty for a \code{rem(method = "nn")} fit by a \strong{stratum
bootstrap}: the case-control strata are resampled with replacement, the
network is refit on each resample (reusing the original \code{\link[=nn_control]{nn_control()}}
settings, including the training \code{engine}), and the spread across refits
yields partial-dependence uncertainty bands and a concordance confidence
interval. This is the inferential counterpart that the point-prediction
\code{nn} backend otherwise lacks (\code{coef()} returns \code{NULL}).
}
\details{
Each bootstrap partial-dependence curve is centred (its grid-mean removed)
before the pointwise quantiles are taken, so the bands describe uncertainty
in the \emph{shape} of each effect, not the conditional-logit's unidentified
per-stratum offset.
}
\seealso{
\code{\link[=rem]{rem()}}, \code{\link[=nn_control]{nn_control()}}
}
