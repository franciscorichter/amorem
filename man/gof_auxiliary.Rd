% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gof.R
\name{gof_auxiliary}
\alias{gof_auxiliary}
\title{GOF test for an auxiliary (unmodelled) statistic}
\usage{
gof_auxiliary(
  event_log,
  model,
  auxiliary,
  n_sim = 1000,
  scope = "all",
  mode = "one",
  half_life = NULL,
  seed = NULL
)
}
\arguments{
\item{event_log}{Dyadic event log.}

\item{model}{Named character vector of \verb{<stat> = "linear"} mapping
for the \emph{fitted} covariates (must not contain \code{auxiliary}).}

\item{auxiliary}{Name of the statistic to test as an unmodelled
feature; must be a statistic computable by
\code{\link[=compute_endogenous_features]{compute_endogenous_features()}}.}

\item{n_sim}{Number of Monte Carlo replicates (default 1000).}

\item{scope, mode, half_life, seed}{See \code{\link[=compare_models]{compare_models()}}.}
}
\value{
List with \code{statistic} (\eqn{T_\phi}{T_φ}), \code{p_value},
\code{G}, \code{u}, and \code{auxiliary}.
}
\description{
Implements the auxiliary-statistic test of Boschi & Wit (2025),
Section 3.7 / eq. 20. Tests whether a covariate \code{auxiliary} that is
\emph{not} part of \code{model} has nonetheless been adequately captured
indirectly by the fitted model. Uses the simulation-based p-value
described in the paper: \code{n_sim} replicates of
\eqn{G^*[\hat\gamma, u]}{G*[γ̂,u]} are drawn from i.i.d. standard
normals, the test statistic
\eqn{T_\phi = \sup_u |G[\hat\gamma, u]|}{T_φ = sup_u |G[γ̂, u]|} is
computed, and the empirical p-value is the fraction of replicates
with \eqn{T_{\phi,b}^* \ge T_\phi}{T*_{φ,b} >= T_φ}.
}
\references{
Boschi M, Wit EC (2025). \emph{Goodness of fit in relational
event models}. Statistics and Computing 36(4).
}
