% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/widen_case_control.R
\name{widen_case_control}
\alias{widen_case_control}
\title{Convert a long case-control event log to wide case-1-control format}
\usage{
widen_case_control(
  data,
  case = NULL,
  stratum = NULL,
  covariates = NULL,
  control_index = 1L,
  keep_ids = TRUE
)
}
\arguments{
\item{data}{A long case-control data.frame.}

\item{case}{Optional name of the 0/1 event-indicator column. If \code{NULL}
(default), it is auto-detected from the package's \code{event} column (as
produced by \code{\link[=sample_non_events]{sample_non_events()}}) or eventnet's \code{IS_OBSERVED}, preferring
\code{event} when both are present.}

\item{stratum}{Optional name of the column grouping each case with its
controls. When \code{NULL}, the stratum is derived as \code{cumsum(case == 1)}
(assuming each case is immediately followed by its controls).}

\item{covariates}{Character vector of covariate columns to widen. When
\code{NULL}, all numeric columns are used except the case indicator, the
stratum, and the standard eventnet bookkeeping columns (\code{EVENT},
\code{INTEGER_TIME}, \code{TIME_POINT}, \code{TIME_UNIT}, \code{EVENT_INTERVAL}).}

\item{control_index}{Which control within each stratum to pair with the case
(default the first). Lets a case-k-control log be reduced to case-1-control.}

\item{keep_ids}{Logical; when \code{TRUE} (default) the sender/receiver identifier
columns present in \code{data} are carried into the output as \code{sender_ev} /
\code{receiver_ev} (the observed event) and \code{sender_nv} / \code{receiver_nv} (the
matched control), so the dyads behind each case-control pair remain
recoverable (and become available to \code{re()} grouping terms in \code{\link[=rem]{rem()}}).
Set to \code{FALSE} to emit only the widened covariate columns.}
}
\value{
A data.frame with one row per case: a \code{stratum} column, the
sender/receiver identifiers (\code{sender_ev}/\code{receiver_ev}/\code{sender_nv}/
\code{receiver_nv}, when present in \code{data} and \code{keep_ids = TRUE}) and, for each
covariate, \verb{<cov>_ev}, \verb{<cov>_nv} and \verb{d_<cov>}. Strata without exactly one
case or without the requested control are dropped (with a message).
}
\description{
Reshapes a long case-(k-)control dataset -- one row per case and per control,
with a 0/1 case indicator -- into a wide \strong{case-1-control} table with one row
per case. For each covariate the event value (\verb{<cov>_ev}), the matched
control value (\verb{<cov>_nv}) and their difference (\verb{d_<cov>}, event minus
control) are emitted, ready for the \code{gam} backend of \code{\link[=rem]{rem()}}.
}
\details{
This is the preprocessing companion to \code{\link[=rem]{rem()}} for eventnet-style output,
where a case row is followed by its controls and the stratum id is left blank
on control rows.
}
\examples{
set.seed(1)
long <- data.frame(
  IS_OBSERVED = rep(c(1, 0, 0), 4),
  x = rnorm(12), y = rnorm(12))
widen_case_control(long, control_index = 1)

}
\seealso{
\code{\link[=rem]{rem()}}, \code{\link[=simulate_relational_events]{simulate_relational_events()}} (\code{wide = TRUE}).
}
