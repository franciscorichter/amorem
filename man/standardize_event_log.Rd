% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/preprocess.R
\name{standardize_event_log}
\alias{standardize_event_log}
\title{Standardize a relational event log}
\usage{
standardize_event_log(
  event_log,
  sender_col = "sender",
  receiver_col = "receiver",
  time_col = "time",
  sort = TRUE,
  drop_nas = TRUE,
  drop_loops = FALSE,
  strictly_increasing_time = FALSE,
  remove_duplicates = TRUE,
  keep_extra = TRUE
)
}
\arguments{
\item{event_log}{A data.frame (or tibble) containing at least one row per
event.}

\item{sender_col, receiver_col, time_col}{Column names storing the sender,
receiver, and time information.}

\item{sort}{Logical; should the output be sorted by time (ties are kept in
input order)?}

\item{drop_nas}{Logical; if \code{TRUE}, rows with missing sender/receiver/time are
removed. Otherwise an error is thrown when NAs are present.}

\item{drop_loops}{Logical; when \code{TRUE}, self-loops (\code{sender == receiver}) are
dropped.}

\item{strictly_increasing_time}{Logical; if \code{TRUE}, an error is raised when
non-increasing time stamps are detected after sorting.}

\item{remove_duplicates}{Logical; drop duplicated combinations of
sender/receiver/time.}

\item{keep_extra}{Logical; if \code{FALSE}, only the standardized columns are
returned. When \code{TRUE}, additional columns from the original input are
preserved.}
}
\value{
A data.frame with columns \code{sender}, \code{receiver}, and \code{time}. The return
object is tagged with class \code{"amore_event_log"} for downstream dispatch.
}
\description{
Module A focuses on preprocessing utilities. This helper normalizes user
supplied event logs into the canonical \code{sender}/\code{receiver}/\code{time} structure
expected elsewhere in the package. It also handles common cleaning tasks such
as sorting, dropping missing rows, and removing loops.
}
\examples{
data(classroom_events)
std <- standardize_event_log(classroom_events)
head(std)
}
